// Quick script to set bart.abraas token for testing
// Run this in the browser console (F12) when on localhost:3000

localStorage.setItem('token', '7af586a944b9f902af4cb20df20411605678d484');
console.log('Token set for bart.abraas - refresh the page to see the dashboard');

// Alternatively, refresh the page automatically
window.location.reload();
